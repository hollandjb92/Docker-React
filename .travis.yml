sudo: required #super user level permissions required
services:
  - docker
before_install:
  - docker build -t hollandjb92/Docker-React -f Dockerfile.dev .

script:
  - docker run -e CI=true hollandjb92/Docker-React npm run test
